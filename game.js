/* Browser application. Rendering and sound consume deterministic combat events. */
(()=>{'use strict';
const T=THREE,{CampaignGame,CampaignState,levels,elements,damageScale,tierFeatures,powerNames,SAVE_KEY,clamp,dist}=SevenWorlds,$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
let campaign=new CampaignState(),stored=false,saveError='';try{const raw=localStorage.getItem(SAVE_KEY);if(raw){campaign=CampaignState.parse(raw);stored=true;}}catch(e){saveError='The stored save could not be read. Import an exported save or start a new game.';}
let renderer;try{renderer=new T.WebGLRenderer({canvas:$('#world'),antialias:false,powerPreference:'high-performance'});}catch(e){$('#unsupported').textContent='WebGL could not start. Enable hardware acceleration in your browser.';$('#unsupported').classList.remove('hidden');return;}
renderer.outputColorSpace=T.SRGBColorSpace;renderer.shadowMap.enabled=true;renderer.shadowMap.type=T.PCFSoftShadowMap;renderer.info.autoReset=false;
const scene=new T.Scene(),camera=new T.PerspectiveCamera(58,innerWidth/innerHeight,.12,260),cinema=new CinematicView(renderer,scene,camera),hero=createActionHero(),heroRoot=new T.Group();heroRoot.add(hero.root);scene.add(heroRoot);
const ambient=new T.HemisphereLight('#bfd3df','#25232a',1.5),sun=new T.DirectionalLight('#cdd5dc',2.1),rim=new T.DirectionalLight('#bddaea',1.4);sun.castShadow=true;sun.shadow.mapSize.set(1024,1024);Object.assign(sun.shadow.camera,{left:-35,right:35,top:45,bottom:-25,near:.5,far:110});sun.shadow.bias=-.001;scene.add(ambient,sun,sun.target,rim,rim.target);
const gl=renderer.getContext(),debugInfo=gl.getExtension('WEBGL_debug_renderer_info'),adapter=debugInfo?gl.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL):'WebGL adapter unavailable';
const damageNumbers=new DamageNumbers($('#damageNumbers'));const swordTrail=new SwordMotion.SwordTrail(scene);const swordFX=new SwordElementFX(scene);const impactFX=new ImpactFX(scene);const fx=new ElementEffects(scene),mastery=new MasteryFX(scene),audio=new ElementAudio(),music=new MusicDirector(audio);let game=new CampaignGame(new CampaignState(),{demo:true,level:0,tier:1}),world=new WorldView(scene,game),ready=false,running=false,paused=true,replay=false,selectionMode='replay',upgradesReturn='menu',time=0,last=performance.now(),fps=60,statsTime=0,frameCount=0,frameTotal=0,autoSamples=0,quality='high',cameraYaw=0,cameraPitch=.39,cameraDistance=13,mouseDown=false,manualCameraUntil=0,impactPulse=0,visualHold=0,queuedCycle=false,toastUntil=0,titleUntil=0,uiTime=0,musicCombatUntil=0,settings={music:.55,effects:.75,reduced:false,quality:'auto',stats:false},keys=new Set(),enemyViews=new Map(),enemyPool=[];
const sharedGeo=new Set(),sharedMat=new Set(),warnGeo=new T.RingGeometry(.94,1,60),warnMat=new T.MeshBasicMaterial({color:'#df7055',side:T.DoubleSide,transparent:true,opacity:.6,depthWrite:false}),warnings=[];for(let i=0;i<30;i++){const g=new T.Group(),ring=new T.Mesh(warnGeo,warnMat),fill=new T.Mesh(new T.CircleGeometry(1,40),new T.MeshBasicMaterial({color:'#d85c3c',side:T.DoubleSide,transparent:true,opacity:.1,depthWrite:false}));ring.rotation.x=fill.rotation.x=-Math.PI/2;g.add(ring,fill);g.visible=false;scene.add(g);warnings.push(g);}
const warningCircle=warnings[0].children[1].geometry;const warningFanShape=new T.Shape();warningFanShape.moveTo(0,0);warningFanShape.lineTo(-16.64,-32);warningFanShape.lineTo(16.64,-32);warningFanShape.closePath();const warningFan=new T.ShapeGeometry(warningFanShape),warningBeam=new T.PlaneGeometry(4.6,32);
const touch=new TouchControls({canvas:$('#world'),onLook:(x,y)=>{if(!paused){manualCameraUntil=time+3;cameraYaw-=x*.006;cameraPitch=clamp(cameraPitch+y*.003,.15,.95);}},onCycle:()=>queuedCycle=true});
try{const s=JSON.parse(localStorage.getItem('fivefold-settings')||'null');if(s)Object.assign(settings,s);}catch{}
$('#musicVolume').value=settings.music*100;$('#effectsVolume').value=settings.effects*100;$('#reduced').checked=settings.reduced;$('#quality').value=settings.quality;$('#showStats').checked=settings.stats;audio.setVolumes(settings.music,settings.effects);
function saveSettings(){try{localStorage.setItem('fivefold-settings',JSON.stringify(settings));}catch{}}
function save(){if(game.demo||replay)return;try{localStorage.setItem(SAVE_KEY,campaign.serialize());stored=true;saveError='';$('#saveStatus').textContent='Checkpoint saved on this device.';}catch(e){saveError='Local saving is unavailable. Use Export Save to keep your progress.';$('#saveStatus').textContent=saveError;toast(saveError);}}
function toast(s){if($('#toast').textContent!==s||time>toastUntil){$('#toast').textContent=s;toastUntil=time+3.5;}}
function overlay(id){$$('.overlay').forEach(o=>o.classList.toggle('hidden',o.id!==id));paused=!!id;keyboard.reset();game.pendingActions={};touch.reset();queuedCycle=false;mouseDown=false;if(id==='home')homeSummary();if(id){const focus=$('#'+id+' .primary:not(:disabled)')||$('#'+id+' button:not(:disabled)');focus?.focus({preventScroll:true});}else $('#world').focus({preventScroll:true});}
function homeSummary(){$('#newGame').textContent='NEW GAME';$('#continue').disabled=!stored||!ready;$('#newGame').disabled=!ready;$('#demo').disabled=!ready;$('#showcase').disabled=!ready;$('#worlds').disabled=!ready;$('#saveSummary').textContent=saveError||(!ready?'Loading character…':stored?`Campaign · ${campaign.completed.length}/7 worlds restored · ${levels[campaign.level].name}`:'A new journey awaits.');}
function disposeEnemy(v){v.root.traverse(o=>{if(o.isSkinnedMesh)o.skeleton.dispose();if(o.isMesh){if(!sharedGeo.has(o.geometry))o.geometry.dispose();if(!sharedMat.has(o.material))o.material.dispose();}});scene.remove(v.root);}
function clearEnemies(){for(const v of enemyViews.values())disposeEnemy(v);enemyViews.clear();for(const v of enemyPool)disposeEnemy(v);enemyPool=[];}
function qualityApply(){quality=settings.quality==='auto'?(matchMedia('(pointer:coarse)').matches?'low':'high'):settings.quality;renderer.setPixelRatio(Math.min(devicePixelRatio,quality==='low'?.9:1.35));renderer.shadowMap.enabled=quality!=='low';resize();}
function resize(){renderer.setSize(innerWidth,innerHeight,false);camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();cinema.resize();}
function start(mode,id=0,tier=1,area='start'){if(!ready)return;audio.start();clearEnemies();world.dispose();fx.reset();impactFX.reset();mastery.reset();swordTrail.reset();swordFX.reset();damageNumbers.reset();const isDemo=mode==='demo',showcase=mode==='showcase',demo=isDemo||showcase;replay=mode==='replay';if(mode==='new'){campaign=new CampaignState();stored=true;}
 let state=replay?CampaignState.parse(campaign.serialize()):demo?new CampaignState():campaign;if(replay){state.level=id;state.snapshot=null;state.unlocked=[0,1,2,3,4];}if(mode==='continue'){id=campaign.level;if(!campaign.snapshot&&campaign.completed.includes(id)&&id<6)id++;}
 const advancing=!demo&&!replay&&id>campaign.level,previousTier=campaign.level+1;game=new CampaignGame(state,{demo,mode:showcase?'showcase':replay?'replay':demo?'demo':'campaign',level:id,tier});game.practice=isDemo&&$('#demoProtect').checked;game.autoCast=isDemo&&$('#demoAutoCast').checked;game.autoSword=isDemo&&$('#demoSword').checked;if(demo&&area!=='start'){if(area==='boss'){game.gates.forEach(g=>g.open=true);game.encounters.forEach(e=>e.done=true);game.orbs.forEach(o=>o.collected=true);game.player.z=game.level.bossTrigger+10;}else{game.gates.filter(g=>g.z<game.level.encounters[0]).forEach(g=>g.open=true);game.player.z=game.level.encounters[0]-25;}game.player.x=game.level.centerAt(game.player.z);}
 world=new WorldView(scene,game);const theme=world.theme;sun.color.set(theme.light);ambient.color.set(theme.light);rim.color.set(theme.glow);cameraYaw=0;cameraPitch=.39;cameraDistance=13;heroRoot.position.set(game.player.x,game.player.y,game.player.z);camera.position.set(game.player.x+4,game.player.y+7,game.player.z-12);music.setWorld(game.level.soundtrack||{exploration:game.level.music,combat:game.level.combat});ambient.intensity=showcase?.8:1.1;sun.intensity=showcase?1.35:1.7;rim.intensity=1.7;musicCombatUntil=0;running=true;overlay(null);if(!demo&&!replay)save();$('#modeBadge').textContent=showcase?'STORM AWAKENING':demo?`DEMO · TIER ${tier}`:replay?'REPLAY':'';$('#chapter').textContent=`${id+1} · ${game.level.name.toUpperCase()}`;$('#worldTitle').textContent=game.level.name;titleUntil=time+4;toast(game.level.story);renderElements();game.events=[];if(advancing){$('#tierTitle').textContent='All five elements · Tier '+game.tier;$('#tierCopy').textContent=`Base damage rises from ${Math.round(damageScale[previousTier-1]*100)}% to ${Math.round(damageScale[game.tier-1]*100)}%. ${tierFeatures[game.tier-1]} unlocked.`;$('#tierPowers').innerHTML=elements.map((el,i)=>`<div style="--el:${el.color}"><b>${el.glyph}</b><span>${el.name}<strong>${powerNames[i][game.tier-1]}</strong></span></div>`).join('');overlay('tierUp');audio.awaken();}}
function renderElements(){$('#elements').innerHTML=elements.map((el,i)=>`<button data-element="${i}" class="${game.element===i?'selected ':''}${game.canUse(i)?'':'locked'}" style="--el:${el.color}" aria-label="${el.name}${game.canUse(i)?', Tier '+game.tier:', locked'}" title="${powerNames[i][game.tier-1]} · ${Math.round(damageScale[game.tier-1]*100)}% base power" aria-pressed="${game.element===i}"><em>${i+1}</em><b>${el.glyph}</b><small>${game.canUse(i)?el.name.toUpperCase():'LOCKED'}</small><small>${game.canUse(i)?'TIER '+game.tier:'◇'}</small></button>`).join('');$$('[data-element]').forEach(b=>b.onclick=()=>{game.select(+b.dataset.element);renderElements();});}
function selection(mode){selectionMode=mode;$('#selectionTitle').textContent=mode==='demo'?'Elemental demo lab':'The seven worlds';$('#selectionCopy').textContent=mode==='demo'?'Choose any world. The Power tier selector overrides campaign progression for testing. Demo play never changes your campaign save.':'Replay unlocked missions. Your main campaign checkpoint is kept separately.';$('#demoOptions').classList.toggle('hidden',mode!=='demo');$('#levelCards').innerHTML=levels.map(l=>{const open=mode==='demo'||l.id<=campaign.highest;return `<button class="level-card" data-level="${l.id}" ${open?'':'disabled'}><span class="num">0${l.id+1}</span><span><strong>${l.name}</strong><small>${l.boss} · TIER ${l.id+1}</small></span><span class="state">${mode==='demo'?'EXPLORE':campaign.completed.includes(l.id)?'RESTORED':open?'UNLOCKED':'LOCKED'}</span></button>`;}).join('');$$('[data-level]').forEach(b=>b.onclick=()=>start(selectionMode,+b.dataset.level,+$('#demoTier').value,$('#demoArea').value));overlay('selection');}
$('#demoTier').innerHTML=Array.from({length:7},(_,i)=>`<option value="${i+1}">Tier ${i+1}${i===6?' · Mastery':''}</option>`).join('');
function upgradeMenu(from){upgradesReturn=from;const allowed=running&&!game.demo&&!replay&&(game.checkpointReady||game.finished);$('#pointsText').textContent=allowed?`${campaign.points} available · ${campaign.spent}/${campaign.earned} points assigned`:'Visit a checkpoint outside combat to redistribute campaign points.';$('#upgradeRows').innerHTML=elements.map((el,i)=>`<div class="upgrade-row"><strong style="color:${el.color}">${el.glyph} ${el.name}</strong><div>${['impact','control'].map(b=>`<button data-upgrade="${i},${b}" ${!allowed||campaign.points===0||campaign.specializations[i][b]>=3?'disabled':''}>${b.toUpperCase()} <b>${campaign.specializations[i][b]}/3</b> +</button>`).join('')}</div></div>`).join('');$$('[data-upgrade]').forEach(b=>b.onclick=()=>{const [i,branch]=b.dataset.upgrade.split(',');if(campaign.upgrade(+i,branch)){save();upgradeMenu(from);}});$('#upgradeAll').disabled=!allowed||campaign.points===0;$('#refund').disabled=!allowed||!campaign.spent;overlay('upgradePanel');}
function newGameClick(){if(stored&&$('#newGame').textContent==='NEW GAME'){$('#newGame').textContent='START FRESH · REPLACES SAVE';toast('Export your current save in Settings if you want to keep it.');}else start('new');}$('#newGame').onclick=newGameClick;$('#continue').onclick=()=>start('continue');$('#worlds').onclick=()=>selection('replay');$('#demo').onclick=()=>selection('demo');$('#showcase').onclick=async()=>{if(!window.buildShowcase){$('#showcase').disabled=true;try{await new Promise((resolve,reject)=>{const script=document.createElement('script');script.src='showcase.js';script.onload=resolve;script.onerror=reject;document.head.append(script);});}catch{toast('Showcase assets could not load. Please retry.');$('#showcase').disabled=false;return;}}start('showcase',0,1);};$('#selectionBack').onclick=()=>overlay('home');$('#homeSettings').onclick=()=>overlay('menu');$('#pause').onclick=()=>{if(running&&!paused)overlay('menu');};$('#resume').onclick=()=>{audio.start();if(running)overlay(null);else overlay('home');};$('#toHome').onclick=()=>{if(!game.demo&&!replay)save();running=false;overlay('home');};$('#upgrades').onclick=()=>upgradeMenu('menu');$('#upgradeBack').onclick=()=>overlay(upgradesReturn);$('#upgradeAll').onclick=()=>{if(running&&!game.demo&&!replay&&(game.checkpointReady||game.finished)&&campaign.upgradeAll()){save();upgradeMenu(upgradesReturn);}};$('#refund').onclick=()=>{campaign.refund();save();upgradeMenu(upgradesReturn);};$('#completeUpgrades').onclick=()=>upgradeMenu('complete');$('#completeHome').onclick=()=>{running=false;overlay('home');};$('#claim').onclick=()=>overlay(null);$('#wieldUpgrade').onclick=()=>overlay(null);$('#nextLevel').onclick=()=>{if(game.showcase){start('showcase',0,1);return;}if(game.level.id===6||replay){running=false;overlay('home');}else start(game.demo?'demo':'next',game.level.id+1,game.demo?Math.min(7,game.tier+1):game.level.id+2);};
$('#export').onclick=()=>{const blob=new Blob([campaign.serialize()],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='fivefold-progress.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);$('#saveStatus').textContent='Save exported. Import this file on another device to continue.';};
$('#import').onchange=async e=>{try{const file=e.target.files[0];if(!file)return;if(file.size>500000)throw Error('Save file is too large.');const next=CampaignState.parse(await file.text());localStorage.setItem(SAVE_KEY,next.serialize());campaign=next;stored=true;running=false;$('#saveStatus').textContent='Save imported. Choose Continue from the main menu.';}catch(error){$('#saveStatus').textContent='Import failed: '+error.message;}e.target.value='';};
$('#musicVolume').oninput=e=>{settings.music=+e.target.value/100;audio.setVolumes(settings.music,settings.effects);saveSettings();};$('#effectsVolume').oninput=e=>{settings.effects=+e.target.value/100;audio.setVolumes(settings.music,settings.effects);saveSettings();};$('#reduced').onchange=e=>{settings.reduced=e.target.checked;saveSettings();};$('#quality').onchange=e=>{settings.quality=e.target.value;qualityApply();saveSettings();};$('#showStats').onchange=e=>{settings.stats=e.target.checked;saveSettings();};$('#touchEnabled').onchange=e=>touch.setEnabled(e.target.checked);$('#sound').onclick=()=>{audio.setEnabled(!audio.enabled);$('#sound').textContent=audio.enabled?'SOUND ON':'MUTED';};
const musicCredits=[['Asian Drums','Kevin MacLeod','https://incompetech.com/music/royalty-free/index.html?Search=Search&isrc=USUAN1100396'],['Spaghetti Western Theme (Orchestral)','Markus Lindner','https://opengameart.org/content/spaghetti-western-theme-orchestral'],['Neon','Scott Buckley','https://www.scottbuckley.com.au/library/neon/'],['AI Fight','Alexander Ehlers','https://opengameart.org/content/t-t-free-cyberpunk-pack-2'],['Curse of the Scarab','Kevin MacLeod','https://incompetech.com/music/royalty-free/index.html?Search=Search&isrc=USUAN1600014'],['The Long Dark','Scott Buckley','https://www.scottbuckley.com.au/library/the-long-dark/'],['Legionnaire (Original)','Scott Buckley','https://www.scottbuckley.com.au/library/legionnaire/'],['The Old Ones','Scott Buckley','https://www.scottbuckley.com.au/library/the-old-ones/'],['Ascension','Scott Buckley','https://www.scottbuckley.com.au/library/ascension/'],['Mountain Emperor','Kevin MacLeod','https://incompetech.com/music/royalty-free/index.html?Search=Search&isrc=USUAN1700012'],['Two Guns, One Destiny','Shane Ivers','https://www.silvermansound.com/free-music/two-guns-one-destiny'],['Five Armies','Kevin MacLeod','https://incompetech.com/music/royalty-free/index.html?Search=Search&isrc=USUAN1100875'],['The Descent','Kevin MacLeod','https://incompetech.com/music/royalty-free/index.html?Search=Search&isrc=USUAN1200094'],['Clash Defiant','Kevin MacLeod','https://incompetech.com/music/royalty-free/index.html?Search=Search&isrc=USUAN1600003']];$('#credits').innerHTML=musicCredits.map(([title,by,url])=>`<div><a href="${url}" target="_blank" rel="noopener">${title}</a> — ${by} · ${by==='Alexander Ehlers'?'CC0':title==='Ascension'?'CC BY 3.0':'CC BY 4.0'}</div>`).join('')+'<p>Lightning impact: <a href="https://opengameart.org/content/thunder" target="_blank" rel="noopener">Thunder by Jerimee</a>, CC BY 3.0. Edited attack, pitch layer, EQ and shortened decay.</p><p>Recordings looped and crossfaded in gameplay. Neon, The Long Dark, Ascension, and Two Guns, One Destiny: Royalty Free Music by free-stock-music.com. Vanguard character & animations: Adobe Mixamo, via Three.js. Rendering: Three.js, MIT. Environment PBR textures: Poly Haven, CC0 — Rob Tuytel, Dario Barresi and Dimitrios Savva. Original architecture, equipment, elemental art and procedural effects.</p>';
function handleEvents(){const events=game.events.splice(0);for(const e of events){if(e.type==='save')save();world.detail?.event(e,time);if(e.type==='warning'){music.duck=.25;audio.layer({type:'triangle',from:e.boss?190:340,to:e.boss?100:220,duration:.16,volume:.1,pan:clamp((e.x-game.player.x)/20,-.8,.8)});}if(e.type==='sword'){audio.layer({noise:true,white:true,cutoff:2400,endCutoff:350,duration:.2,volume:.13});}if(e.type==='swordcontact'){music.duck=.3;impactPulse=Math.max(impactPulse,e.strength);visualHold=e.combo===3?.045:.02;audio.layer({noise:true,cutoff:600,endCutoff:90,duration:.24,volume:.22});}if(e.type==='transformation'){audio.awaken();impactPulse=1;renderElements();$('#worldTitle').textContent=e.tier===4?'THE STORM AWAKENS':'MASTER OF THE FIVE';titleUntil=time+3;}if(e.type==='finishready'){toast('The Warden is broken. Release and press CAST to restore the city.');$('#worldTitle').textContent='UNLEASH THE STORM';titleUntil=time+5;}if(e.type==='finale'){fx.handOrigins=hero.hands();fx.cast({...e,type:'cast',ultimate:true,range:25,impactX:e.x,impactZ:e.z},time,25);audio.cast(e.element,true);impactPulse=1;sun.intensity=2.2;ambient.intensity=1.5;world.detail.restored=true;}if(e.type==='worldchange'){toast(e.name);audio.awaken();}if(e.type==='cast'){if(!e.sword){fx.handOrigins=hero.hands();fx.cast(e,time,e.range);audio.cast(e.element,e.ultimate);}else {audio.layer({noise:true,white:true,cutoff:e.combo===3?1500:3000,endCutoff:250,duration:e.combo===3?.32:.19,volume:e.combo===3?.2:.14});if(e.combo===3)audio.layer({noise:true,cutoff:190,endCutoff:65,duration:.26,volume:.12});}}if(e.type==='impact')audio.impact(e.element);if(e.type==='enemyattack')audio.layer({noise:true,cutoff:500,endCutoff:90,duration:.35,volume:.1});mastery.event(e,time);impactFX.event(e,time);swordFX.event(e,time);if(e.type==='notice'||e.type==='wave'||e.type==='opened')toast(e.text);if(e.type==='switch')renderElements();if(e.type==='damage')damageNumbers.event(e,time);if(e.type==='unlocked'){audio.awaken();$('#unlockTitle').textContent=elements[e.element].name+' awakens.';$('#unlockCopy').textContent=['Gusts scatter enemies and tear away veils.','Ignite enemies. Burning foes erupt on defeat and spread flames.','Thunder strikes briefly paralyze enemies and build guardian stagger.','Water marks enemies. Defeat a marked foe with any element to restore health.','Stone interrupts enemy attacks and briefly reduces incoming damage.'][e.element];$('#crystalArt').style.setProperty('--el',elements[e.element].color);renderElements();overlay('unlock');}if(e.type==='checkpoint'){audio.awaken();toast('Checkpoint reached · health restored · specializations available');}if(e.type==='mastery'){audio.awaken();toast('Mastery shrine restored.');}if(e.type==='reaction')toast(e.name.toUpperCase()+' · ELEMENTAL REACTION');if(e.type==='boss')toast(e.name+' · Guardian of the fracture');if(e.type==='bossphase')toast(e.name+' · Phase II');if(e.type==='respawn'){fx.reset();impactFX.reset();mastery.reset();swordTrail.reset();swordFX.reset();damageNumbers.reset();clearEnemies();world.dispose();world=new WorldView(scene,game);save();toast('Restored at your checkpoint. Earned elements and upgrades are kept.');}if(e.type==='complete'){audio.awaken();$('#completeLabel').textContent=e.final?'THE SEVEN WORLDS RESTORED':'PASSAGE RESTORED';$('#completeTitle').textContent=e.final?'The fracture closes.':game.level.name;$('#completeCopy').textContent=e.final?'The stolen worlds return to their own skies. Your journey is complete. Replay any mission or explore Tier VII in the demo lab.':game.demo?'Guardian defeated. Continue exploring the next world.':replay?'Mission replay complete. Your campaign checkpoint is unchanged.':`The ${game.level.boss} falls. Five specialization points earned. Use Specializations → Upgrade All to strengthen every element. All five elements advance to Tier ${Math.min(7,game.tier+1)} in the next world.`;$('#nextLevel').textContent=e.final||replay?'RETURN TO MAIN MENU':'ENTER THE NEXT WORLD';$('#completeUpgrades').disabled=game.demo||replay;if(game.showcase){$('#completeLabel').textContent='STORM AWAKENING · COMPLETE';$('#completeTitle').textContent='The city breathes again.';$('#completeCopy').textContent='A glimpse of Tempest Walker: Storm of Ruins. Five elements. One unbroken blade.';$('#nextLevel').textContent='REPLAY SHOWCASE';$('#completeUpgrades').disabled=true;}overlay('complete');}}
}
function enemyVisual(e){let v=enemyViews.get(e.id);if(v)return v;v=hero.makeEnemy(e.role==='boss'?10:e.id,game.level.theme,e.role,elements[e.weakness]?.color);v.root.scale.setScalar(e.role==='boss'?2.45:1.45);scene.add(v.root);enemyViews.set(e.id,v);return v;}
function drawEnemies(){const p=game.player;for(const e of game.enemies){if(!GuardianView.visible(e,p))continue;if(e.hp<=0){const v=enemyViews.get(e.id);if(v){v.update(time,false,{...e,simTime:game.time});v.deathTime??=time;v.root.rotation.x=Math.min(1.5,(time-v.deathTime)*3);v.root.position.y=-(time-v.deathTime)*.6;if(time-v.deathTime>1.4){disposeEnemy(v);enemyViews.delete(e.id);}}continue;}const v=enemyVisual(e);v.root.visible=true;const moving=v.lastX!==undefined&&Math.hypot(e.x-v.lastX,e.z-v.lastZ)>.005;v.root.position.set(e.x+(e.hitDX||0)*e.hit*1.3,game.walkHeight(e.x,e.z),e.z+(e.hitDZ||0)*e.hit*1.3);v.root.rotation.set(0,Math.atan2(p.x-e.x,p.z-e.z),0);v.update(time,moving,{...e,simTime:game.time});const toCameraX=camera.position.x-p.x,toCameraZ=camera.position.z-p.z,projection=((e.x-p.x)*toCameraX+(e.z-p.z)*toCameraZ)/(toCameraX*toCameraX+toCameraZ*toCameraZ||1),obscures=projection>0&&projection<1&&Math.hypot(e.x-p.x-projection*toCameraX,e.z-p.z-projection*toCameraZ)<(e.role==='boss'?3.2:1.5);v.root.traverse(o=>{if(o.isMesh&&o.material){o.material.transparent=obscures;o.material.opacity=obscures?.22:1;o.material.depthWrite=!obscures;}});v.lastX=e.x;v.lastZ=e.z;}for(const [id,v]of enemyViews){const e=game.enemies.find(e=>e.id===id);if(!e||Math.abs(e.z-p.z)>70){disposeEnemy(v);enemyViews.delete(id);}}}
function drawWarnings(){for(let i=0;i<warnings.length;i++){const mesh=warnings[i],t=game.telegraphs[i];mesh.visible=!!t;if(!t)continue;mesh.position.set(t.x,game.walkHeight(t.x,t.z)+.065,t.z);mesh.rotation.y=0;const progress=1-t.life/t.max;if(t.kind==='fan'||t.kind==='beam'){mesh.rotation.y=Math.atan2(t.dx,t.dz);mesh.children[0].visible=false;mesh.children[1].geometry=t.kind==='fan'?warningFan:warningBeam;mesh.children[1].position.set(0,0,t.kind==='fan'?0:16);mesh.scale.set(1,1,1);}else{mesh.children[0].visible=true;mesh.children[1].geometry=warningCircle;mesh.children.forEach(m=>m.position.set(0,0,0));mesh.scale.setScalar(t.kind==='melee'?2.2:t.r);}mesh.children[1].material.opacity=.06+progress*.16;}}
$('#guardianGuide').onclick=()=>{const b=game.boss;cameraYaw=Math.atan2(b.x-game.player.x,b.z-game.player.z);cameraPitch=.39;manualCameraUntil=time+3;$('#world').focus({preventScroll:true});};
function updateGuardianGuide(){const b=game.boss,el=$('#guardianGuide');if(paused||b.hp<=0||Math.abs(b.z-game.player.z)>65){el.classList.add('hidden');return;}const v=new T.Vector3(b.x,game.walkHeight(b.x,b.z)+5,b.z).project(camera),off=v.z<0||v.z>1||Math.abs(v.x)>.8||Math.abs(v.y)>.8;el.classList.toggle('hidden',!off);if(off){const a=Math.atan2(b.x-game.player.x,b.z-game.player.z)-cameraYaw;el.textContent=(Math.sin(a)<0?'◀ ':'▶ ')+b.name.toUpperCase()+' · '+Math.round(dist(b,game.player))+' m';}}
function updateThreats(){const el=$('#threats');el.replaceChildren();if(paused)return;for(const t of game.telegraphs.slice(0,4)){const v=new T.Vector3(t.ox,3,t.oz).project(camera);if(v.z>-1&&v.z<1&&Math.abs(v.x)<.9&&Math.abs(v.y)<.85)continue;const mark=document.createElement('i');mark.textContent='▲';const a=Math.atan2(t.ox-game.player.x,t.oz-game.player.z)-cameraYaw;mark.style.left=(50+Math.sin(a)*40)+'%';mark.style.top=(50-Math.cos(a)*35)+'%';mark.style.transform=`rotate(${a}rad)`;el.append(mark);}}
function hud(){const p=game.player;$('#powerGrowth').textContent=game.canUse(game.element)?`${powerNames[game.element][game.tier-1]} · TIER ${game.tier} · ${Math.round(damageScale[game.tier-1]*(1+game.specs[game.element].impact*.15)*100)}% POWER${game.element===0?(game.airPrimedUntil>game.time?' · NEXT CAST: CRITICAL':' · '+Math.round(game.airCritChance*100)+'% CRIT ×2'):''}`:'FIND AN ELEMENTAL CRYSTAL';$('#healthFill').style.width=Math.max(0,p.hp)+'%';$('#healthText').textContent=`${Math.ceil(p.hp)} / 100${game.shield>0?' · STONE GUARD':''}`;$('#checkpointText').textContent=game.checkpointReady?'CHECKPOINT · UPGRADES READY':'';$('#progress').style.width=game.progress*100+'%';let goal;if(game.currentOrb&&game.currentOrb.z<=((game.currentGate?.z)||Infinity))goal=`Find the ${elements[game.currentOrb.type].name} crystal · ${Math.ceil(dist(p,game.currentOrb))} m`;else if(game.inCombat)goal=game.boss.active&&game.boss.hp>0?(game.relays.some(r=>r.hp>0)?`Destroy the relay pylons · ${game.relays.filter(r=>r.hp>0).length} remaining`:'Defeat the '+game.boss.name+'.'):'Defeat the district guards. Prioritize support units.';else if(game.currentGate)goal=game.currentGate.label+' · '+elements[game.currentGate.type].name;else goal=game.showcase&&!game.inCombat&&game.showcaseStage<2?(game.showcaseStage===0?'Enter the rain garden. Test your blade and elements.':'Enter the flooded courtyard. Water gathers; lightning conducts.'):'Reach the guardian court.';if(game.signature&&!game.signature.done&&Math.abs(p.z-game.signature.z)<28){const need=game.level.id===5&&game.signature.phase===1?3:(game.level.signature?.element??3);goal=game.level.signature?.title||'Flood the courtyard';goal+=' · '+elements[need].name;}if(game.finishReady)goal='Release and press CAST · Restore the city';$('#objectiveText').textContent=goal;const enc=game.encounters.find(e=>e.started&&!e.done);$('#stage').textContent=enc?`ENCOUNTER ${enc.id+1} · WAVE ${enc.wave}/${game.level.waves}`:`WORLD ${game.level.id+1} · TIER ${game.tier}`;
const boss=game.boss;$('#bossHud').classList.toggle('hidden',!boss.active||boss.hp<=0);$('#bossName').textContent=boss.name;$('#bossName').title='Weak to '+elements[boss.weakness].name+' (+50% damage)';$('#bossPhase').textContent=`GUARDIAN · PHASE ${boss.phase} / II`;$('#bossFill').style.width=Math.max(0,boss.hp/boss.max*100)+'%';$('#staggerFill').style.width=boss.stagger+'%';const target=game.acquireTarget({x:p.dx,z:p.dz});let shown=false;if(target){const point=new T.Vector3(target.x,game.walkHeight(target.x,target.z)+(target.role==='boss'?7.3:5.2),target.z).project(camera);if(point.z<1&&point.z>-1&&Math.abs(point.x)<1&&Math.abs(point.y)<1){shown=true;$('#targetLock').style.left=(point.x*.5+.5)*innerWidth+'px';$('#targetLock').style.top=(-point.y*.5+.5)*innerHeight+'px';$('#targetName').textContent=target.kind==='gate'?elements[target.type].name.toUpperCase():target.kind==='relay'?'RELAY':target.role.toUpperCase();const weakness=elements[target.weakness];$('#targetWeakness').classList.toggle('hidden',!weakness);$('#targetWeakness').textContent=weakness?`${weakness.glyph} ${weakness.name.toUpperCase()} +50%`:'';if(weakness)$('#targetWeakness').style.color=weakness.color;$('#targetFill').style.width=target.hp/target.max*100+'%';}}$('#targetLock').classList.toggle('hidden',!shown||paused);
}
const keyboard=new KeyboardControls({keys,isPlaying:()=>running&&!paused,onGesture:()=>audio.start(),onAction:code=>{
 if(/^(Digit|Numpad)[1-5]$/.test(code)){game.select(+code.slice(-1)-1);renderElements();}
 if(code==='Tab')queuedCycle=true;
 if(code==='KeyM')$('#sound').click();
 if(code==='KeyR'){cameraYaw=0;cameraPitch=.39;cameraDistance=13;manualCameraUntil=time+3;}
},onEscape:()=>{
 if(!paused){if(running)overlay('menu');return;}
 const active=$('.overlay:not(.hidden)')?.id;
 if(active==='menu'){audio.start();overlay(running?null:'home');}
 else if(active==='selection')overlay('home');
 else if(active==='upgradePanel')overlay(upgradesReturn);
 else if(active==='unlock'||active==='tierUp')overlay(null);
},onSuspend:()=>{mouseDown=false;touch.reset();queuedCycle=false;if(running&&!paused)overlay('menu');}});
window.addEventListener('resize',resize);$('#world').addEventListener('contextmenu',e=>e.preventDefault());$('#world').addEventListener('pointerdown',e=>{if(e.button===2){mouseDown=true;$('#world').setPointerCapture(e.pointerId);}});for(const event of ['pointerup','pointercancel','lostpointercapture'])$('#world').addEventListener(event,()=>mouseDown=false);$('#world').addEventListener('pointermove',e=>{if(mouseDown&&!paused){manualCameraUntil=time+3;cameraYaw-=e.movementX*.006;cameraPitch=clamp(cameraPitch+e.movementY*.003,.15,.95);}});$('#world').addEventListener('wheel',e=>{cameraDistance=clamp(cameraDistance+e.deltaY*.008,7,22);e.preventDefault();},{passive:false});
function frame(now){requestAnimationFrame(frame);const dt=Math.min(.1,(now-last)/1000);last=now;time+=dt;frameCount++;frameTotal+=dt;const p=game.player;if(running&&!paused){if(keys.has('KeyQ')||keys.has('KeyF'))manualCameraUntil=time+3;if(keys.has('KeyQ'))cameraYaw+=dt*1.5;if(keys.has('KeyF'))cameraYaw-=dt*1.5;const axes=keyboard.axes(),move=cameraMovement(axes.x+touch.x,axes.z+touch.y,cameraYaw),c=Math.cos(cameraYaw),s=Math.sin(cameraYaw);if(game.demo&&game.practice)game.player.invincible=1;game.advance(dt,{view:{x:camera.position.x,z:camera.position.z,dx:s,dz:c,tan:Math.tan(camera.fov*Math.PI/360)*camera.aspect},mx:move.mx,mz:move.mz,cast:keyboard.takePress('Space')||keys.has('Space')||touch.casting||game.autoCast,tempest:keyboard.takePress('KeyE')||keys.has('KeyE')||touch.tempest,sword:keyboard.takePress('KeyC')||keys.has('KeyC')||touch.sword||game.autoSword,cycle:queuedCycle});queuedCycle=false;handleEvents();}
heroRoot.position.set(game.player.x,game.player.y,game.player.z);heroRoot.rotation.y=Math.atan2(game.player.dx,game.player.dz);visualHold=Math.max(0,visualHold-dt);impactPulse=Math.max(0,impactPulse-dt*5);hero.update(time,{...game.player,hold:visualHold,enchanted:game.canUse(game.swordAnim>0?game.swordElement:game.element),swordAnim:game.swordAnim,swordDuration:game.swordDuration,combo:game.combo,moving:paused?0:game.player.moving},!paused&&game.castAnim>0,game.swordAnim>0?game.swordElement:game.element);const swordPose=SwordMotion.pose(game.combo||1,1-game.swordAnim/(game.swordDuration||.34));const bladeElement=game.swordAnim>0?game.swordElement:game.element,bladeSegment=ready&&game.canUse(bladeElement)?hero.swordSegment():null;swordTrail.update(time,ready?hero.swordSegment():null,!paused&&game.swordAnim>0&&swordPose.trail,bladeElement,game.combo,settings.reduced);swordFX.update(time,bladeSegment,bladeElement,!paused&&game.swordAnim>0,game.swordAnim>0?game.swordTier:game.tier,settings.reduced||quality==='low',game);fx.handOrigins=hero.hands();fx.update(time,settings.reduced||quality==='low');mastery.update(time,game,settings.reduced);impactFX.update(time,game,settings.reduced||quality==='low');if(ready)drawEnemies();drawWarnings();world.update(time,settings.reduced?'low':quality);
const liveRelay=game.boss.active?game.relays.filter(e=>e.hp>0&&dist(e,game.player)<=32&&game.visibleTarget(e)).sort((a,b)=>dist(a,game.player)-dist(b,game.player))[0]:null;const selected=[...game.enemies,...game.relays].find(e=>e.id===game.targetId&&e.hp>0),lock=GuardianView.focus(game,selected||liveRelay);if(lock&&time>manualCameraUntil&&!paused&&game.player.moving<.1){const desiredYaw=Math.atan2(lock.x-game.player.x,lock.z-game.player.z),delta=Math.atan2(Math.sin(desiredYaw-cameraYaw),Math.cos(desiredYaw-cameraYaw));cameraYaw+=clamp(delta,-.7*dt,.7*dt);}const framedDistance=GuardianView.distance(cameraDistance,camera.aspect,lock?.role==='boss')+(!paused&&game.castAnim>0&&game.element===3?4:0);const aim=new T.Vector3(game.player.x,game.player.y+3.3,game.player.z),desired=aim.clone().add(new T.Vector3(-Math.sin(cameraYaw)*Math.cos(cameraPitch)*framedDistance,Math.sin(cameraPitch)*framedDistance,-Math.cos(cameraYaw)*Math.cos(cameraPitch)*framedDistance));const collision=world.cameraPosition(aim,desired);camera.position.lerp(collision,1-Math.exp(-dt*10));camera.lookAt(aim.clone().add(new T.Vector3(Math.sin(cameraYaw)*5,1.1,Math.cos(cameraYaw)*5)));sun.position.set(p.x-25,42,p.z-15);sun.target.position.set(p.x,0,p.z+12);rim.position.set(p.x+12,10,p.z+18);rim.target.position.set(p.x,1,p.z);
damageNumbers.update(time,camera,innerWidth,innerHeight,paused,settings.reduced);music.update(dt,running&&!paused,game.inCombat);if(running&&!paused&&audio.context&&time>audio.ambienceAt){audio.ambienceAt=time+2.5;audio.layer({noise:true,cutoff:game.level.id===4?1100:450,duration:3,attack:.6,volume:game.level.id===2?.006:.025});}updateThreats();updateGuardianGuide();renderer.info.reset();if(quality==='low'){renderer.toneMapping=T.ACESFilmicToneMapping;renderer.toneMappingExposure=1.1;renderer.render(scene,camera);}else{renderer.toneMapping=T.NoToneMapping;cinema.render(time,settings.reduced,impactPulse*.35);}if(time-uiTime>.12){uiTime=time;hud();}$('#toast').classList.toggle('show',time<toastUntil);$('#worldTitle').classList.toggle('show',running&&!paused&&time<titleUntil);$('#stats').classList.toggle('hidden',!settings.stats);if(time-statsTime>1){fps=frameCount/frameTotal;frameCount=0;frameTotal=0;statsTime=time;if(settings.quality==='auto'&&running&&!paused&&fps<42&&quality==='high'){autoSamples++;if(autoSamples>4){quality='low';renderer.setPixelRatio(.9);renderer.shadowMap.enabled=false;resize();toast('Graphics adjusted for smoother play.');}}$('#stats').textContent=`${fps.toFixed(1)} FPS · ${quality} · ${innerWidth}×${innerHeight}\n${renderer.info.render.calls} draws · ${(renderer.info.render.triangles/1000).toFixed(0)}k triangles\nGPU: ${renderer.info.memory.geometries} geometries / ${renderer.info.memory.textures} textures\nFX: ${fx.jobs.length} jobs / ${mastery.jobs.length} accents\nAudio: ${audio.voices} voices / ${audio.decks.length} streams${audio.musicError?'\n'+audio.musicError:''}\n${adapter}`;}}
new FullscreenControls({onChange:()=>{resize();if(running&&!paused)$('#world').focus({preventScroll:true});},onHelp:message=>{overlay('menu');$('#fullscreenHelp').textContent=message;$('#fullscreenHelp').classList.remove('hidden');}});
qualityApply();renderElements();homeSummary();hero.ready.then(()=>{ready=true;heroRoot.scale.setScalar(1.45);hero.root.traverse(o=>{if(o.isMesh){sharedGeo.add(o.geometry);sharedMat.add(o.material);}});homeSummary();}).catch(e=>{$('#saveSummary').textContent='Character loading failed. Check that the complete game folder was downloaded.';console.error(e);});requestAnimationFrame(frame);
})();
